import React from 'react'

const ServerSideRendering = () => {
  return (
    <div className='mb-44 md:mb-64 lg:mb-72'>
      <h1 className='text-2xl text-center font-bold mt-10'>Server Side Rendering</h1>
      <p className='text-center mt-6'>Server Side Rendering(SSR) is a web development technique where the content of a webpage is generated on the server and then sent
      as fully rendered HTML to the client(browser).In SSR, the server processes the requested page, renders the necessary content, 
      and sends it to the browser, which can immediately display the fully-formed page.</p>
    </div>
  )
}

export default ServerSideRendering;
